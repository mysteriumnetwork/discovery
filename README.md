# discovery

## Build

```
mage build
```

Or using docker:

```
docker build -t discovery:local .
```

## Run

```
mage up
```

Or using docker:

```
docker-compose up
```

API: http://localhost:3000/api/v3
